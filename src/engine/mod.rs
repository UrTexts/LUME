// src/engine/mod.rs

pub mod game;     // Handles game state and logic
pub mod camera;   // Manages camera movement and projection
pub mod input;    // Handles user input (keyboard, mouse)
pub mod math;     // Provides utility functions and structs (e.g., matrices, vectors)
pub mod renderer;
pub mod physics;
pub mod menu;

